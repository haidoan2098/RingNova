# kernel/Makefile
#
# Stub — kernel objects are currently compiled by boot/Makefile
# directly for the boot phase (single-pass link).
#
# This file will grow in Phase 2 (Exception) and beyond as the
# kernel gains its own subsystems (mm, proc, sched, syscall).

.PHONY: all clean

all:
	@echo "[kernel] No standalone kernel target yet — built via boot/Makefile"

clean:
	@echo "[kernel] Nothing to clean yet"
